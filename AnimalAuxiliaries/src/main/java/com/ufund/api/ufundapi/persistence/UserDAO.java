package com.ufund.api.ufundapi.persistence;

public interface UserDAO {

    //Should be pretty minimal, really just exists so that the managers and the helpers can be stored in the same place, I think? 
    
    //TO DO getUsername 

    //TO DO getID ? 
}
